// Tab 7: Pipeline Triggers (scaffold - filled by agent A2)
const PipelineTab = {
  async load() {
    const container = document.getElementById('tab-pipeline');
    App.renderLoading(container);
  }
};
